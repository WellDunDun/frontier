# Legacy Frontier companion job state / local scratch (never committed)
.frontier/

# Local Claude agent worktrees (never committed)
.claude/worktrees/

# Local environment files
.env
.env.*
!.env.example

# Node
node_modules/
